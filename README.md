# VPN Клиент с интеграцией Telegram Mini App

Это современное VPN-приложение с интеграцией Telegram Mini App, состоящее из трех основных компонентов:

1. **Frontend**: React-приложение с использованием современных технологий
2. **Backend**: Python-сервер на Flask с API для управления VPN-подключениями и встроенным Mini App
3. **Telegram Bot**: Бот на aiogram с интеграцией Mini App

## Особенности

- 🚀 **Быстрое подключение** к серверам по всему миру
- 🔐 **Безопасность и конфиденциальность** данных пользователя
- 📱 **Интеграция с Telegram** через Mini App и бота
- 🌐 **Поддержка нескольких серверов** в разных регионах
- 💡 **Простота использования** благодаря интуитивному интерфейсу
- ⚡ **Реальное время** отслеживания статуса подключения
- 🛡️ **Защита от утечек IP-адреса** и DNS-запросов
- 📊 **Мониторинг подключения** с детальной информацией

## Технологии

### Frontend
- **React** - библиотека JavaScript для создания пользовательских интерфейсов
- **styled-components** - библиотека для стилизации компонентов с помощью CSS-in-JS
- **Axios** - библиотека для выполнения HTTP-запросов
- **Telegram Web Apps SDK** - официальный SDK для интеграции с Telegram Web Apps

### Backend
- **Python** - основной язык программирования
- **Flask** - микрофреймворк для создания веб-приложений
- **Flask-CORS** - расширение для обработки CORS
- **JSON** - формат обмена данными между клиентом и сервером

### Telegram Bot
- **aiogram** - асинхронный фреймворк для создания Telegram-ботов
- **aiohttp** - асинхронная HTTP-библиотека для взаимодействия с API
- **Python-dotenv** - библиотека для загрузки переменных окружения из .env файлов

### Инструменты разработки
- **npm** - менеджер пакетов для JavaScript
- **pip** - менеджер пакетов для Python
- **Git** - система контроля версий
- **localtunnel/ngrok** - инструменты для проброса портов и HTTPS-доступа

## Архитектура проекта

```
vpn/
├── react_frontend/           # React-приложение
│   ├── node_modules/         # Зависимости проекта
│   ├── public/               # Публичные файлы
│   ├── src/                  # Исходный код
│   │   ├── components/       # Компоненты React
│   │   ├── pages/            # Страницы приложения
│   │   ├── utils/            # Вспомогательные утилиты
│   │   └── App.js            # Главный компонент
│   ├── package.json          # Зависимости frontend
│   ├── package-lock.json     # Блокировка версий зависимостей
│   └── .gitignore            # Файлы, игнорируемые Git
├── backend/                  # Бэкенд на Flask
│   ├── config/               # Конфигурационные файлы
│   ├── models/               # Модели данных
│   ├── routes/               # Маршруты API
│   ├── services/             # Сервисы приложения
│   ├── templates/            # Шаблоны HTML
│   ├── utils/                # Вспомогательные утилиты
│   ├── server.py             # Основной сервер с Mini App
│   ├── requirements.txt      # Зависимости backend
│   └── .gitignore            # Файлы, игнорируемые Git
├── bot/                      # Telegram-бот
│   ├── config/               # Конфигурация бота
│   ├── handlers/             # Обработчики команд и сообщений
│   ├── middlewares/          # Промежуточные обработчики
│   ├── utils/                # Вспомогательные утилиты
│   ├── bot.py                # Основной файл бота
│   ├── requirements.txt      # Зависимости бота
│   └── .gitignore            # Файлы, игнорируемые Git
├── __pycache__/              # Кэшированные Python файлы
├── requirements.txt          # Общие зависимости
├── .env.example              # Пример файла переменных окружения
├── .gitignore                # Файлы, игнорируемые Git
└── README.md                 # Документация проекта
```

## Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/ваш-аккаунт/vpn-client-miniapp.git
cd vpn-client-miniapp
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
cd react_frontend && npm install
cd ../bot && pip install -r requirements.txt
```

### 3. Настройка переменных окружения

Создайте файл `.env` в корне проекта, используя шаблон из `.env.example`:

```
TELEGRAM_BOT_TOKEN=ваш_токен_бота
BACKEND_URL=http://23.134.216.190:5000
SECRET_KEY=ваш_секретный_ключ
MINI_APP_URL=http://23.134.216.190:5000/miniapp
API_VERSION=v1
```

### 4. Запуск backend-сервера

```bash
cd backend
python server.py
```

Сервер будет доступен по адресу `http://23.134.216.190:5000`, а Mini App по адресу `http://23.134.216.190:5000/miniapp`

### 5. Настройка HTTPS для локальной разработки

Telegram Web Apps требует HTTPS для работы. Для локальной разработки используйте ngrok или localtunnel:

#### Вариант 1: ngrok
1. Установите ngrok: `npm install -g ngrok` или скачайте с официального сайта
2. Запустите ngrok для проброса порта: `ngrok http 5000`
3. Скопируйте HTTPS URL из вывода ngrok
4. Обновите URL в боте, заменив `https://your-ngrok-domain.ngrok.io/miniapp` на ваш URL

#### Вариант 2: localtunnel
1. Установите localtunnel: `npm install -g localtunnel`
2. Запустите backend-сервер: `cd backend && python3 server.py`
3. В другом терминале запустите localtunnel: `lt --port 5000`
4. Скопируйте полученный HTTPS URL (вида https://xxxxx.loca.lt)
5. Обновите переменную MINI_APP_URL в файле .env, заменив `https://your-localtunnel-url.loca.lt/miniapp` на ваш URL
6. Перезапустите бота

### 6. Запуск React-приложения

```bash
cd react_frontend
npm start
```

Приложение будет доступно по адресу `http://23.134.216.190:3000`

### 7. Запуск Telegram-бота

```bash
cd bot
python bot.py
```

## Особенности реализации

### Frontend (React)
- Современный UI с использованием styled-components
- Интеграция с Telegram Web Apps API
- Адаптивный дизайн
- Взаимодействие с backend через REST API

### Backend (Flask)
- REST API для управления VPN-подключениями
- Маршруты для получения статуса, подключения и отключения
- Поддержка нескольких серверов
- Встроенный Mini App по адресу `/miniapp`
- CORS для взаимодействия с фронтендом

### Telegram Bot (aiogram)
- Команды для управления VPN-подключениями
- Inline-клавиатуры для удобства использования
- Интеграция с backend для получения данных
- Поддержка Mini App через Web App кнопки

### Telegram Mini App
- Встроенный в бэкенд HTML-интерфейс с интеграцией Web Apps API
- Возможность запуска прямо в Telegram через Web App кнопки
- Взаимодействие с backend через API
- Отображение статуса подключения в реальном времени

## Использование

1. Запустите бэкенд-сервер
2. Найдите вашего Telegram-бота и отправьте команду `/start`
3. Нажмите кнопку "📱 Открыть приложение" для запуска Mini App прямо в Telegram
4. Используйте интерфейс Mini App для управления VPN-подключениями
5. Также можно использовать команды бота для управления подключениями

### Настройка HTTPS для Telegram Web App

Telegram Web Apps требует HTTPS URL для корректной работы. Для локальной разработки используйте localtunnel:

1. Установите localtunnel: `npm install -g localtunnel`
2. Запустите backend-сервер: `cd backend && python3 server.py`
3. В другом терминале запустите localtunnel: `lt --port 5000`
4. Скопируйте полученный HTTPS URL (вида https://xxxxx.loca.lt)
5. Обновите переменную MINI_APP_URL в файле .env, заменив `https://your-localtunnel-url.loca.lt/miniapp` на ваш URL
6. Перезапустите бота

## API endpoints

### Основные эндпоинты
- `GET /` - главная страница с информацией о сервисе
- `GET /health` - проверка статуса сервера
- `GET /miniapp` - открытие Telegram Mini App

### API для управления подключением
- `GET /api/users/{user_id}/status` - получение статуса подключения пользователя
- `POST /api/users/{user_id}/connect` - подключение пользователя к VPN
- `POST /api/users/{user_id}/disconnect` - отключение пользователя от VPN

### API для получения информации
- `GET /api/servers` - получение списка доступных серверов
- `GET /api/users/{user_id}/info` - получение информации о пользователе (в разработке)

### Примеры использования API
Для всех API-запросов, кроме `/health`, требуется передача `user_id` в URL. 
При подключении к VPN через POST-запрос `/api/users/{user_id}/connect` можно указать сервер:
```json
{
  "server": "US-East"
}
```

## Примеры использования

### Через Telegram-бота
1. Найдите вашего Telegram-бота и отправьте команду `/start`
2. Нажмите кнопку "📱 Открыть приложение" для запуска Mini App прямо в Telegram
3. Используйте интерфейс Mini App для управления VPN-подключениями
4. Также можно использовать команды бота для управления подключениями:
   - `/status` - проверить статус подключения
   - `/connect` - подключиться к VPN
   - `/disconnect` - отключиться от VPN

### Через веб-интерфейс
1. Откройте Mini App по адресу `http://23.134.216.190:5000/miniapp` (или через HTTPS URL при использовании ngrok/localtunnel)
2. Выберите сервер из списка
3. Нажмите "Подключиться" для активации VPN-соединения
4. Мониторьте статус подключения в реальном времени

## Вклад в развитие проекта

Мы приветствуем вклад в развитие проекта! Чтобы внести свой вклад:

1. Сделайте форк репозитория
2. Создайте ветку для новой функции (`git checkout -b feature/new-feature`)
3. Внесите изменения и зафиксируйте их (`git commit -m 'Add new feature'`)
4. Загрузите изменения в удалённый репозиторий (`git push origin feature/new-feature`)
5. Отправьте pull request